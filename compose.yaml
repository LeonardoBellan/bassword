name: bassword-server

services:
  server:
    build: .
    image: bassword-server
    volumes:
    - bassword-volume:/app/data/
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      - DB_PATH=/app/data/bassword.db

volumes:
  bassword-volume:
